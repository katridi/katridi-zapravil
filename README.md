# Катриди заправил

Репозиторий подкаста **«Катриди заправил»**.

В этом подкасте мои рассказы звучат голосами разных людей.

Здесь хранятся сайт подкаста, метаданные сезонов и эпизодов.
RSS-лента генерируется автоматически при сборке.

## Архитектура

```text
GitHub
│
├── сайт
├── метаданные сезонов и эпизодов
└── RSS
     ↓
GitHub Pages
     ↓
сайт + feed.xml

Cloudflare R2
     ↓
MP3
```

RSS используется для публикации подкаста в:

- Яндекс Музыке
- Spotify
- Apple Podcasts
- YouTube / YouTube Music

## Структура репозитория

```text
katridi-zapravil/
│
├── seasons/
│   └── season-01/
│       ├── episode-01/
│       │   └── episode.yml
│       ├── episode-02/
│       │   └── episode.yml
│       └── ...
│
├── assets/
│   └── cover-placeholder.svg
│
├── scripts/
│   ├── build.py
│   └── build_feed.py
│
├── tests/
│   └── ...
│
├── podcast.yml
├── requirements.txt
├── index.html
├── style.css
├── README.md
├── AGENTS.md
└── .gitignore
```

Основная структура проекта строится по номеру сезона и эпизода, а не по названию рассказа.

Название рассказа хранится внутри `episode.yml`.

## Выпуски

Первый выпуск:

```text
seasons/
└── season-01/
    └── episode-01/
        └── episode.yml
```

Пример `episode.yml`:

```yaml
title: "Щенок"

season: 1
season_title: "Воспоминания"
episode: 1
episode_type: "full"

author: "Алексей Катриди"
reader: "Сергей Глебкин"

guid: "kz-s01e01"

status: "draft"
published_at: null

audio:
  url: null
  type: "audio/mpeg"
  duration: null
  length: null

description: >
  Рассказ Алексея Катриди.
  Читает Сергей Глебкин.
```

После публикации `guid` выпуска нельзя менять.

Название рассказа можно изменить без изменения директории, URL или GUID.

## URL эпизодов

Предпочтительный формат URL:

```text
/s01/e01/
/s01/e02/
/s02/e01/
```

URL не должен зависеть от названия рассказа.

Это позволяет менять название эпизода, не ломая внешние ссылки.

## Аудио

Архивный мастер:

```text
WAV
mono
48 kHz
24-bit
```

Дистрибуционный файл:

```text
MP3
mono
48 kHz
CBR 192 kbps
```

MP3 не хранятся в GitHub.

Они загружаются в Cloudflare R2.

Предпочтительная структура:

```text
s01/
├── e01.mp3
├── e02.mp3
└── e03.mp3

s02/
├── e01.mp3
└── ...
```

Пример URL:

```text
https://audio.example.com/s01/e01.mp3
```

## Обложка

Основная обложка:

```text
3000 × 3000 px
JPG или PNG
без прозрачности
```

До получения финальной обложки используется placeholder.

Название должно оставаться читаемым в маленьком размере.

## Публикация нового выпуска

1. Подготовить финальный MP3.
2. Загрузить его в Cloudflare R2.
3. Создать директорию нового эпизода:

```text
seasons/season-01/episode-02/
```

4. Добавить в неё `episode.yml`.
5. Проверить:
   - `season`;
   - `episode`;
   - `guid`;
   - название;
   - описание;
   - дату публикации;
   - URL аудио.
6. Поменять `status` на `published`, когда выпуск готов.
7. Сделать commit и push в `main`.
8. GitHub Actions проверит метаданные, соберёт `dist/` и опубликует сайт через GitHub Pages.

После обновления RSS новый выпуск должен автоматически поступить на подключённые платформы.

## Локальная сборка

Production-сборку можно воспроизвести локально:

```bash
python3 scripts/build.py
```

Команда очищает `dist/`, копирует публичные статические файлы и генерирует `dist/feed.xml`.

Тесты запускаются так:

```bash
python3 -m unittest discover -s tests
```

`feed.xml` и `dist/` являются сгенерированными файлами и не должны редактироваться вручную.

## GitHub Pages

Публикация настроена через GitHub Actions без коммита сгенерированного `feed.xml` обратно в `main`.

В настройках репозитория нужно выбрать:

```text
Settings → Pages → Build and deployment → Source → GitHub Actions
```

После этого push в `main` будет запускать проверку, сборку и деплой Pages artifact из `dist/`.

## Важные правила

- не менять `guid` опубликованного эпизода;
- не переименовывать директории опубликованных сезонов и эпизодов без необходимости;
- не удалять опубликованный MP3;
- не менять URL старого аудиофайла без проверки RSS;
- не привязывать постоянные URL к названию рассказа;
- не хранить в репозитории пароли, API-токены или другие секреты.

При необходимости секреты хранятся в GitHub Secrets.

## RSS

Production RSS должен иметь постоянный публичный URL.

Например:

```text
https://katridizapravil.com/feed.xml
```

При будущем переезде на другой podcast-hosting старый RSS должен отдавать постоянный HTTP `301` redirect на новый feed.

## Сайт

На сайте размещаются:

- описание проекта;
- список сезонов;
- список эпизодов каждого сезона;
- страницы отдельных эпизодов;
- ссылки на Яндекс Музыку;
- Spotify;
- Apple Podcasts;
- YouTube / YouTube Music;
- дополнительные ссылки проекта.

## Авторские права

Репозиторий опубликован без open-source лицензии.

Если явно не указано иное, все права на тексты, рассказы, аудиозаписи, изображения, обложки, название проекта и другие авторские материалы сохраняются за их правообладателями.

Публичность репозитория не означает разрешения на копирование, распространение, переработку или коммерческое использование его содержимого.

Технический код проекта в будущем может быть опубликован под отдельной лицензией.
